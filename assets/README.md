# Sprite assets

Put base sprite images in `base`.

Put variant sprite images in folders named after the variant type, for example:

- `Gold`
- `Shadow`
- `Crystal`

The tracker stores paths like `assets/base/water.png` and `assets/Gold/GoldWater.png`.
